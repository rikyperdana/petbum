# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

# Built in Libraries
meteor-base@1.4.0            # Packages every Meteor app needs to have
mobile-experience@1.0.5      # Packages for a great mobile UX
mongo@1.6.0                  # The database Meteor supports right now
static-html                  # Define static page content in .html files
reactive-var@1.0.11          # Reactive variable for tracker
tracker@1.2.0                # Meteor's client-side reactive programming library
standard-minifier-css@1.5.2  # CSS minifier run for production mode
standard-minifier-js@2.4.0   # JS minifier run for production mode
es5-shim@4.8.0               # ECMAScript 5 compatibility for older browsers
ecmascript@0.12.4            # Enable ECMAScript2015+ syntax in app code
shell-server@0.4.0           # Server-side component of the `meteor shell` command
less@2.8.0                   # Leaner CSS language

# Additional libraries
zenkogu:livescript-compiler  # The best javascript descendant
lenville:bulma               # CSS framework with no JS fuss
stevezhu:lodash              # So good I don't wanna code without it
aldeed:collection2@=2.0.1    # Data structure validator
aldeed:autoform@=5.8.1       # Needed only to allow autoform prop in schema
momentjs:moment              # To deal with date/time formatting
accounts-password            # Password based user logging system
harrison:papa-parse          # To encode csv file to be processed
alanning:roles               # Users roling system
nilsdannemann:pdfmake        # Client-sided pdf creation library
numeral:numeral              # General purpose number formating
lfergon:exportcsv            # Decode informations back to csv
sakulstra:aggregate          # Allows aggregation command in server-side
izzilab:shelljs              # Allow shell command execution on server-side
mrt:cron                     # Simplest CRON job at server-side
